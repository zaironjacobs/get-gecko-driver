name: Run tests

on:
  schedule:
    - cron: "0 */12 * * *"
  push:
    branches: [ "main" ]
    paths-ignore:
      - '**.md'
  pull_request:
    branches: [ "main" ]
    paths-ignore:
      - '**.md'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.14'

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Install get-gecko-driver
        run: |
          pip install .

      - name: Install test dependencies
        run: |
          pip install --group test

      - name: Run tests
        run: |
          pytest tests
